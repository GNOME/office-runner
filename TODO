- Icons that match the license?
- Sound?
- Start the counter when the lid gets closed

Known bugs (aka, can I be bothered?)
- saving the date/time as a string means it won't get
  retranslated if we switch languages
